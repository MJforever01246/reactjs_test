import {
    makeStyles
  } from '@material-ui/core';
  export const useStyles = makeStyles({
    test: {
      color: 'red',
      backgroundColor: '#000'
    },
    test2: {
      backgroundColor: 'red'
    },
    test3:{
      flexFlow: 'column wrap',
      backgroundColor: 'red'
    }
  });